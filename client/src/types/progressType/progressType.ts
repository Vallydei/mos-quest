export type ProgressType = {
  id: number;
  userId: number;
  questionId: number;
  complete: boolean;
};

export type ProgressTypeData = {
  userId: number;
  questionId: number;
  complete: boolean;
};

